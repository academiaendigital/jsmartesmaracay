var alumno = "jenny";
console.log(alumno);
var alumnos = ['luis', 'eddy', 'carlos', 'jeramy', 'daniel'];
console.log(alumnos[0]);
console.log(alumnos.length);
for (var i = 0; i < alumnos.length; i++) {
  // console.log(i)
  console.log(`El nombre del participante es ${alumnos[i]} y ocupa la posicion ${i} de la lista`);
}

